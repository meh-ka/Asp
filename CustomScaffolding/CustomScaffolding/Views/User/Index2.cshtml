@model IEnumerable<CustomScaffolding.Models.UserModel>

@{
    ViewData["Title"] = "Index2";
}

<h1>Eléve</h1>

<p>
    <a asp-action="Create">Créer nouveau</a>
</p>
<table class="table table-success table-striped">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Family)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Family)
            </td>
            <td>
                @Html.ActionLink("Modifier", "Modifier", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Détails", "Details", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("supprimer", "Delete", new { /* id=item.PrimaryKey */ })
            </td>
        </tr>
}
    </tbody>
</table>
